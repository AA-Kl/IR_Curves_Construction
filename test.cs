using System;

namespace IR_Curves_Construction // Note: namespace depends on the project name.
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("hihi, its test of .net coding in vs code (old version)");
        }
    }
}